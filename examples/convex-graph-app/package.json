{
  "name": "tsift-convex-graph-app",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone Convex app deploying the tsift graph backend schema, mutations, and HTTP action. Used as the live-deployment target for `tsift convex-sync --remote-snapshot --apply` and the `--confirmed-convex-reconciled` half of #gdbvacproof.",
  "type": "module",
  "scripts": {
    "dev": "convex dev",
    "deploy": "convex deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "convex": "^1.16.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
