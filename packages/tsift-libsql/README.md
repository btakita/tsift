# tsift-libsql

libSQL graph store backend for tsift.

This crate implements the shared GraphStore contract against local or remote
libSQL/Turso databases. It is part of the versioned tsift workspace and is
published with the rest of the split crates.
