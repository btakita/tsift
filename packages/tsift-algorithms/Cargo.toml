[package]
name = "tsift-algorithms"
version = "0.1.0"
edition = "2024"
description = "Graph algorithms for tsift — Tarjan SCC, health scoring, dead code detection, coupling analysis"
license = "MIT"
repository = "https://github.com/btakita/tsift"
authors = ["Brian Takita <brian.takita@gmail.com>"]

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
tsift-core = { path = "../tsift-core" }
