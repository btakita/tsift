# tsift-status

Session health and lock diagnostics for tsift.

This crate powers `tsift status` and lock diagnostics for index freshness,
instruction versions, summary-cache state, and SQLite sidecar locks. It is part
of the versioned tsift workspace and is published with the rest of the split
crates.
