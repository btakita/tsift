# tsift-sim-world

Deterministic SimWorld test harness for tsift.

This crate provides seeded simulation fixtures and named-edge traces over the
session, rewrite, and status surfaces. It is part of the versioned tsift
workspace and is published with the rest of the split crates.
