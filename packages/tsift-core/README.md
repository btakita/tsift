# tsift-core

Provider-neutral graph types for tsift.

This crate defines graph records, provenance/freshness metadata, the GraphStore
trait, and shared query helpers used by tsift graph backends. It is part of the
versioned tsift workspace and is published with the rest of the split crates.
