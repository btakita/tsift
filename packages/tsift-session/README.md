# tsift-session

Session observability compatibility shim for tsift.

This crate re-exports the `tsift-agent-doc` session cost, digest, and review
surfaces for existing consumers. It is part of the versioned tsift workspace and
is published with the rest of the split crates.
