# tsift-summarize

Cached LLM analysis foundation for tsift.

This crate owns the summary database, cached entity/concept/relationship reads,
and snapshot-fallback reader used by search and digest surfaces. It is part of
the versioned tsift workspace and is published with the rest of the split
crates.
