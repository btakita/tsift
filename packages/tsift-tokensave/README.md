# tsift-tokensave

Tokensave database reader adapter for tsift.

This crate maps `.tokensave/tokensave.db` nodes and edges into tsift graph
records and exposes read-only GraphStore queries. It is part of the versioned
tsift workspace and is published with the rest of the split crates.
