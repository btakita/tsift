# tsift-cache

Shared cache primitives for tsift and agent-doc integrations.

This crate owns reusable disk-cache contracts that should not require callers
to depend on higher-level quality gates, CLI code, or evaluation surfaces. It is
part of the versioned tsift workspace and is published with the rest of the
split crates.
